### Documents protégés et application

- `INVARIANTS.md`

#### Commande d’application

```bash
docforge apply [PATH] DOCUMENTS [--owner-approved] [--allow-dirty]
```

- Option `--owner-approved` (facultatif)
- Option `--allow-dirty` (facultatif)

Statut : fait détecté.
