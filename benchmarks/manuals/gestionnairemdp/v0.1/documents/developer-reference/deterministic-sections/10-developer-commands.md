La référence complète des cibles Make d’exploitation est publiée dans le **Guide d’exploitation**.

#### `test-backend`

Exécute la cible Make `test-backend` détectée.

```bash
make test-backend
```

Effet de bord : Suppression de fichiers.

#### `test-frontend`

Exécute la cible Make `test-frontend` détectée.

```bash
make test-frontend
```

Effet de bord : Suppression de fichiers.

#### `token-test`

Exécute la cible Make `token-test` détectée.

```bash
make token-test
```
